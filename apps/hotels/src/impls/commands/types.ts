export enum Command {
  Start = '/start',
}

export enum Step {
  FirstTopic,
  FirstTopicTest,

  SecondTopic,
  SecondTopicTest,

  Finish,

  // Don't edit, add integer values only before this
  MAX,
}
